name: start
run-name: start_flow

on:
  push:
    branches:
      - main
jobs:
  main:
    runs-on: ubuntu-latest

    steps:
      - name: set env variable
        run: |
          echo "MY_VAR=hello" >> $GITHUB_ENV
          echo "$MY_VAR"
